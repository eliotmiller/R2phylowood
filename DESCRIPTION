#this will be a small library made to take a nexus file & a data frame of traits where the
#first and second columns are the latitude and longtiude, respectively, of the centroid
#of the species' ranges in the phylogeny. will then return the components necessary to
#build an nhx file for use in phylowood

readTreeBlock <- function(file)
{
	require(ape)
	temp.nex <- scan(file = file, what = "", sep = "\n", quiet = TRUE)
	treeBlock <- temp.nex[length(temp.nex)-1]
	return(treeBlock)
}
